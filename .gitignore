node_modules/
site/dist/
tools/nostr/out/
*.log
.DS_Store
# Signing keys never belong in the registry. The only exception is the clearly
# marked throwaway demo key under tools/osc/test/, which is committed on purpose.
*.pem
id_ed25519
id_rsa

# Launch drafts / private runbooks are kept out of the public repo
docs/launch/
docs/MIGRATION.md

# Local session memory / private disclosure material — never commit.
# Unredacted findings under embargo live here; publishing one would breach
# policy/DISCLOSURE.md and hand attackers a live vulnerability.
.local/
private/
